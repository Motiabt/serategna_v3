# API base. In dev, leave blank to use the Vite proxy (/api -> :4000).
VITE_API_BASE=

# Google Maps JavaScript API key. Leave blank to use the built-in styled
# fallback map (the location picker still works without a key).
VITE_GOOGLE_MAPS_KEY=
