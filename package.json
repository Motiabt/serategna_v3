{
  "name": "serategna",
  "version": "3.0.0",
  "private": true,
  "description": "Serategna — Phase 1 marketplace (API + web). Run both with one command.",
  "scripts": {
    "install:all": "npm install && npm --prefix server install && npm --prefix web install",
    "setup": "npm --prefix server run setup",
    "dev:api": "npm --prefix server run dev",
    "dev:web": "npm --prefix web run dev",
    "dev": "concurrently -n API,WEB -c green,cyan \"npm:dev:api\" \"npm:dev:web\"",
    "build": "npm --prefix web run build",
    "start:api": "npm --prefix server start",
    "first-run": "npm run install:all && npm run setup && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
